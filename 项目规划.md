**一、任务需求总结**

这个项目的目标是基于 C++ 开发一款 LLM-based 智能体，用于向用户推荐团购服务。它不是单纯的对话机器人，而是一个“理解用户需求 + 调用工具 + 检索候选 + 排序推荐 + 生成解释”的完整智能体系统。

核心需求可以概括为以下几类：

- 用户通过自然语言输入需求，例如“帮我推荐今晚适合三个人吃的海鲜团购，预算 300 左右”。
- 系统要先理解用户意图，识别出类目、预算、人数、时间、城市、偏好等槽位。
- 如果关键信息不足，系统要先追问，而不是直接胡乱推荐。
- 如果信息足够，系统要调用召回模块，从团购商品库中筛出候选项。
- 系统要调用排序模块，对候选项做打分和排序。
- 系统要输出 Top N 推荐，并给出清晰的推荐理由。
- 系统要记录会话、用户反馈、推荐日志，用于后续优化。
- 整个系统需要具备合规控制、风险过滤、异常兜底和观测评估能力。

如果按产品目标来讲，这个 Agent 的本质是：把用户的模糊需求转成结构化推荐任务，再通过 LLM 和推荐工具链完成闭环。

**二、项目架构总结**

前面设计的架构是分层式的，目标是把“大模型理解能力”和“推荐执行能力”解耦，让系统既灵活又可控。

整体链路是：

- 用户输入请求。
- API 接入层接收请求。
- Agent 编排层解析意图、判断是否追问、规划工具调用。
- 工具层执行召回、排序、解释、外部能力查询。
- 记忆层管理会话上下文和用户画像。
- 策略与安全层负责合规、风控和兜底。
- 观测与评估层记录日志、指标、链路和实验结果。

架构上分成以下几个重点模块：

- API 接入层，负责 HTTP 或 gRPC 请求处理、鉴权、限流和参数校验。
- Agent 编排层，负责统一调度整条业务链路，是系统大脑。
- LLM 接入层，负责模型调用、Prompt 构造、响应解析、降级切换。
- 工具层，负责召回、排序、解释和外部服务接入。
- 召回层，负责从商品库中筛候选。
- 排序层，负责根据特征和规则对候选打分。
- 记忆层，负责会话状态和用户偏好管理。
- 策略与安全层，负责敏感内容过滤、违规控制、失败兜底。
- 数据访问层，负责数据库和索引读写。
- 观测与评估层，负责日志、指标、追踪和 A/B 实验。

这个架构的关键思想有两个：

- LLM 不直接负责最终推荐结果，而是负责理解、规划、生成解释。
- 推荐结果必须由检索、排序、规则系统共同决定，这样更稳定、可解释、可扩展。

**三、C++ 工程结构总结**

工程目录被拆成多个职责明确的模块，适合在 C++ 中逐步实现和维护。

总体目录结构是：

- apps，放服务入口，例如 api_server、worker、admin_console。
- include，放头文件接口声明。
- src，放各模块实现。
- proto，放接口定义。
- configs，放配置文件。
- sql，放建表和迁移脚本。
- tests，放测试代码。
- third_party，放第三方依赖。

模块划分上，核心实现包括：

- agent，处理编排、会话管理、任务规划、响应组装、工具路由。
- llm，处理模型调用、Prompt 构造、结果解析、模型降级。
- tools，统一封装各类可调用工具。
- retriever，负责候选召回。
- ranker，负责打分和排序。
- memory，负责会话记忆和用户画像。
- policy，负责安全和策略控制。
- data，负责数据库访问。
- observability，负责日志、指标和评估。
- common，负责公共类型、状态码和配置。

这个结构的优点是清晰分层，尤其适合后面逐步扩展：先做最小闭环，再加画像、评估、AB 实验和更多工具。

**四、头文件级接口设计总结**

前面已经把接口细化到了接近真实 C++ 头文件的骨架，核心是围绕最小可运行闭环来设计的。

公共基础类型主要集中在 common 层：

- UserContext，描述用户上下文，例如 user_id、session_id、city、language、device_type。
- SlotValue，表示槽位提取结果。
- IntentResult，表示意图识别结果。
- ClarificationQuestion，表示追问内容。
- RecommendationItem，表示单个推荐商品。
- RecommendationResult，表示最终推荐结果。
- ToolCallRequest 和 ToolCallResult，表示工具调用请求和返回。
- LlmRequest 和 LlmResponse，表示模型请求与响应。
- Status 和 ErrorCode，表示统一错误处理。

API 层的主要接口包括：

- ApiServer，负责启动和注册路由。
- ChatController，负责处理聊天、推荐、反馈和会话查询请求。
- ChatRequest、ChatResponse、FeedbackRequest，负责请求响应数据结构。
- 中间件层负责鉴权、限流和 trace 传递。

Agent 编排层的主要接口包括：

- AgentOrchestrator，负责整条任务链路的调度。
- ConversationManager，负责会话状态管理。
- TaskPlanner，负责意图解析、槽位提取、追问判断和任务计划生成。
- ResponseComposer，负责自然语言回复生成。
- ToolRouter，负责把工具调用请求路由到具体工具。

LLM 层的主要接口包括：

- LlmClient，封装大模型调用、流式输出、健康检查和超时重试。
- PromptBuilder，负责构建不同阶段的 Prompt。
- LlmResponseParser，负责把模型输出解析成结构化结果。
- ModelFallbackManager，负责模型失败时的降级选择。

工具层的主要接口包括：

- ITool，作为所有工具的统一基类。
- RetrieverTool，负责候选召回。
- RankTool，负责排序。
- ExplainTool，负责推荐理由生成。
- ExternalTool，负责地图、营业时间、优惠券等外部信息查询。

召回层的主要接口包括：

- RetrieverService，负责从商品库中召回候选。
- RetrievalRequest 和 RetrievalResult，描述召回输入输出。
- IndexQuery 和 FilterChain，用于索引查询和过滤链组合。

排序层的主要接口包括：

- RankerService，负责候选排序。
- FeatureExtractor，负责抽取用户、商品、上下文特征。
- Scorer，负责打分函数。
- RankingPolicy，负责业务规则和多样性控制。
- RankedItem 和 RankedList，描述排序输出。

记忆层的主要接口包括：

- SessionMemoryStore，负责短期会话记忆。
- UserProfileStore，负责长期用户画像。
- PreferenceExtractor，负责从对话中抽取偏好。
- ConversationSession、ConversationTurn、UserProfile，作为核心数据结构。

策略层的主要接口包括：

- PolicyEngine，负责请求、推荐和输出的策略检查。
- SafetyFilter，负责敏感内容过滤。
- FallbackResponder，负责系统异常时兜底回复。

数据访问层的主要接口包括：

- SessionRepository，负责会话表。
- MessageRepository，负责消息表。
- ItemRepository，负责团购商品表。
- MerchantRepository，负责商家表。
- RecommendationLogRepository、FeedbackLogRepository、LlmCallLogRepository，负责日志数据。

观测层的主要接口包括：

- Logger，负责日志输出。
- MetricsReporter，负责指标统计。
- Tracer，负责链路追踪。
- Evaluator，负责推荐效果评估。
- ExperimentManager，负责 A/B 实验。

**五、数据表设计总结**

为了让推荐 Agent 能真正落地，前面设计了最小可用的数据表集合。

核心数据表包括：

- users，存用户基础信息。
- user_profiles，存用户画像。
- sessions，存会话状态。
- conversation_messages，存对话内容和结构化意图。
- groupbuy_items，存团购商品主数据。
- merchants，存商家信息。
- item_features，存排序特征。
- recommendation_logs，存推荐日志。
- feedback_logs，存用户反馈。
- llm_calls，存大模型调用审计。

这些表的作用分别是：

- 支撑对话上下文。
- 支撑推荐召回和排序。
- 支撑画像和个性化。
- 支撑日志、审计和评估。

**六、推荐系统调用链总结**

在线请求的推荐流程建议如下：

- 用户发起请求。
- API Server 接收并校验请求。
- AgentOrchestrator 读取会话和用户画像。
- TaskPlanner 解析意图和槽位。
- 如果缺少关键槽位，则先追问。
- 如果槽位齐全，则调用 RetrieverService 召回候选。
- RankerService 对候选做排序。
- PolicyEngine 做安全与业务检查。
- ResponseComposer 生成自然语言回复。
- 写入推荐日志、会话日志和反馈埋点。

这个流程强调“先理解，再执行，再解释”，是整个项目最重要的逻辑骨架。

**七、MVP 范围总结**

为了尽快做出可演示版本，建议第一阶段只实现最小闭环：

- API Server。
- AgentOrchestrator。
- TaskPlanner。
- LlmClient。
- RetrieverService。
- RankerService。
- SessionMemoryStore。

这个最小闭环就能实现：

- 用户输入一句自然语言。
- 系统理解需求。
- 系统召回候选。
- 系统排序推荐。
- 系统生成回复。
- 系统记录会话。

如果要评分，这个方案的优点是：

- 架构分层清晰。
- 任务边界明确。
- C++ 接口设计已经接近可落地实现。
- 兼顾了 LLM、推荐、记忆、工具调用和安全控制。
- 适合逐步扩展，不会一开始就陷入大而全。

如果你要我继续，我可以下一步把这份总结再压缩成一页适合打分的“方案评审稿”，或者展开成“评分维度表”，方便另一个智能体按维度逐项评价。