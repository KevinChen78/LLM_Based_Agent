# 通告:执行窗口职责确认

> 发出方:架构规划窗口 | 日期:2026-08-27 | 生效:立即

## 协作格局(三窗口 + 用户)

```
执行窗口(你) ──完成汇报──▶ PM窗口 ──验收报告落盘──▶ 架构规划窗口 ──plan草稿──▶ 用户确认 ──▶ 执行窗口
                          (三层实证验收)  docs/acceptance/        (轮询触发,起草plan)      (你的唯一任务来源)
```

- **执行窗口(你)**:唯一实现方
- **PM 窗口**:验收方,不写实现代码
- **架构规划窗口**:只写规划 plan,不写实现代码、不做验收实证
- **用户**:决策方——plan 确认、需求优先级、争议裁决

## 你的职责

1. **任务来源唯一**:只执行用户确认后的 plan 文件(`C:\Users\Kevin Chen\.claude\plans\*.md` 或用户在对话中明确指定的规划文档)。plan 之外的新增需求、方向变更,一律请用户先过规划窗口,不要直接接活。
2. **按 plan 实现 + 自测**:实现代码、跑构建和单测自证。遵循 CLAUDE.md 的全部工程约定(构建脚本、测试从项目根跑、Windows 环境坑等)。
3. **实现细节自主**:函数命名、内部组织、代码风格由你定,plan 不会管到这个粒度。

## 你的边界(不可越过)

1. **契约稳定面不得擅自改**:降级链、retrieval_service 三端点 JSON 形状、Python stdlib 策略、观测回传通道模式(store 指针不穿组件签名)。plan 未显式标注的契约变更,先停下来报用户裁决。
2. **不写规划文档**:发现"下一步该做什么"的问题,记录并汇报,由规划窗口处理。
3. **不自行宣布"阶段完成"**:完成实现后向用户汇报——改动文件清单、自测命令与结果、已知遗留。是否通过由 PM 窗口独立验收判定。

## 当前状态

- 基线 commit `64de433` = 已验收态。
- 未提交改动属 Phase 2.1/2.2(experiment_manager / ranker_client / ranking_service 等,对应 plan `cheeky-wondering-wand.md` 阶段 A~F),实现进行中,完成后进入 PM 窗口验收。

## 记忆提示

建议将本通告要点存入你的项目记忆(type: feedback):任务来源唯一性、契约稳定面上报义务、"不自行宣布完成"。
