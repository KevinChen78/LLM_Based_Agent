# 验收报告:Phase 7 阶段 A — 高德 POI 拉取脚本

- **结论**:✅ 通过(附 1 条观察)
- **验收日期**:2026-08-27
- **验收人**:PM 验收窗口(三层实证验收)

## 验收范围

- **基线**:`2dd9439`(v1.0 终验);验收对象 = commit `acd24fb`(+ 工作区未提交的 around-search 扩展,一并在测)
- **对应 plan**:`phase7-poi-real-data.md` 阶段 A
- **涉及文件**:`scripts/fetch_pois.py`、`data/pois_cache/`、`data/pois_raw.json`(均 gitignored 数据产物)

## 规划层核对

| 验收点(plan 阶段 A) | 结果 | 证据 |
|---|---|---|
| 纯 stdlib、确定性 | ✅ | 脚本仅 urllib/json/os 等;输出按 poi_id 排序,原子写(temp+os.replace) |
| 无 key 报错指引明确、不动 deals.json | ✅(实证) | 置空 AMAP_KEY 运行:**exit=2**,无文件写入;deals.json git 状态无变化 |
| `--limit` 试跑产出合法 | ✅ | data/pois_raw.json 实存(279KB,867 个去重 POI,执行窗口真实拉取产物) |
| 连跑两次零网络请求 | ✅(实证) | 重跑日志逐页标「缓存」,末尾「本次网络请求 0 次」 |
| 中断续跑不费配额 | ✅(代码级) | 失败页不落缓存(避免毒化),已缓存页永久命中;配额熔断保留已缓存部分 |
| key 零硬编码 | ✅ | 环境变量/.env.local 读取,gitignore 覆盖 |

## 契约层核对

- stdlib 策略 ✅;deals.json 不被本阶段触碰 ✅;数据目录 gitignored 合规 ✅

## 实证层(本窗口独立实跑)

| 命令 | 结果 |
|---|---|
| 置空 key 运行 | ✅ exit=2,明确指引,零写入 |
| 重跑(缓存命中) | ✅ 0 网络请求,867 POI 输出稳定 |
| 缓存/配额文件实查 | ✅ 40+ 页缓存落盘;_quota.json 日期+计数结构正确 |

## 差距清单

| # | 现象 | 整改建议 | 严重度 |
|---|---|---|---|
| 1 | 我重跑前后 _quota.json 计数 39→48(+9),而该次运行自报 0 网络请求。代码审查确认 `used+=1` 仅在非缓存路径(fetch_pois.py:228-231),逻辑正确;最可能解释是执行窗口在我两次读数之间并行跑了一轮真实拉取(9 个新页)。**无法完全排除计数异常**,但证据倾向并行活动 | 若执行窗口确认当时有并行拉取,此项关闭;否则查 load/save_quota 竞态(单进程无并发写,理论上安全) | 低 |

## 下一阶段建议

阶段 B(gen_poi_deals 合成)的关键验收点已就绪:类目映射覆盖率报告、id 4xxxxx 区间隔离、幂等连跑逐字节一致。工作区 around-search 扩展建议尽早 commit 或并入阶段 B,避免基线漂移。
