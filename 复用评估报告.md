# 现有基础设施复用评估报告

## 一、可复用资产清单

| 资产 | 位置 | 评估 | 复用建议 |
|---|---|---|---|
| C++20 协程库 `coro` | `coro/include/coro/` | **可直接复用**。提供 `Task<T>`、`Generator<T>`、`ThreadPool`、`AsyncMutex`、`Channel`、内存池等基础设施，满足 Phase 0 异步编排需求。 | 作为 Agent 控制面的核心异步运行时。 |
| HTTP Server | `coro/include/coro/net/http/server.hpp` | **可直接复用**。支持路由、`GET/POST`、路径参数、`Request/Response`、JSON/HTML 响应、协程 Handler。 | 作为 API Server 的 HTTP 入口，无需引入 Drogon。 |
| HTTP Request/Response | `coro/include/coro/net/http/request.hpp` `response.hpp` | **可直接复用**。Request 已解析 query/body/header，Response 提供 json/html/text/status 等 builder。 | 用于 `/v1/chat`、`/v1/feedback`、`/v1/history` 实现。 |
| 线程池 | `coro::ThreadPool` | **可直接复用**。 | 驱动 HTTP Server 和异步任务调度。 |
| webserver 项目 | `webserver/` | **不建议复用**。该项目偏传统 WebServer 教学项目（FTP、KV 存储、日志、静态文件），与 LLM Agent 推荐系统架构差异较大。 | 仅参考其 CMake 组织方式；不依赖 `webserver` 库。 |

## 二、关键结论：无需引入 Drogon

原修订版方案中提到 `Drogon / boost::asio`，但鉴于 `coro` 库已提供：

- C++20 协程 `Task<T>`
- 协程 HTTP Server + Router
- Request/Response 解析与构建
- ThreadPool / AsyncMutex / Channel

**Phase 0 可以直接基于 `coro` 构建 HTTP 服务层，无需引入 Drogon**，减少依赖和学习成本。

## 三、仍需自行补充的部分

| 缺失能力 | 说明 | 建议方案 |
|---|---|---|
| JSON 库 | `coro` 只提供手写 JSON helper，不适合生产 | 引入 `nlohmann/json` 或 `rapidjson` |
| gRPC/Protobuf | `coro` 未提供 | 继续使用 gRPC + Protobuf，生成 C++ 客户端/服务 |
| HTTP Client | `coro` 只有服务端，没有异步 HTTP Client | 方案 A：基于 `coro::io::TcpStream` 自研简单 HTTP Client；方案 B：引入 `cpr`/`httplib` 做同步调用，外部包一层 `co_await run_on_thread_pool` |
| Redis 客户端 | 无 | `hiredis` + `hiredis-vip` 或自研简单异步封装 |
| PostgreSQL 客户端 | 无 | `libpqxx` 同步库，外部包 `run_on_thread_pool` |
| LLM Gateway 通信 | 无 | Python 侧实现 Gateway，C++ 侧用 HTTP/gRPC 调用 |
| 日志 / 指标 | `coro` 无 | 复用 `webserver` 的 `AsyncLogger` 或引入 `spdlog` |

## 四、推荐调整后的 Phase 0 技术栈

| 层次 | 原方案 | 基于现有资产调整 |
|---|---|---|
| C++ 协程运行时 | Drogon / boost::asio | `coro::ThreadPool` + `coro::Task<T>` |
| HTTP Server | Drogon | `coro::net::http::Server` |
| HTTP Client | Drogon 自带 | 自研简单 Client 或 `cpr`/`httplib` |
| JSON | - | `nlohmann/json` |
| gRPC | gRPC + Protobuf | 不变 |
| 日志 | - | `spdlog` 或 `webserver::AsyncLogger` |
| Redis | - | `hiredis` |
| PostgreSQL | - | `libpqxx` |

## 五、复用 `coro` HTTP Server 的 Phase 0 入口示例

```cpp
#include "coro/coro.hpp"
#include <nlohmann/json.hpp>

using namespace coro;
using namespace coro::net::http;
using json = nlohmann::json;

class AgentApplication {
public:
    Task<void> run(uint16_t port) {
        ThreadPool pool(4);
        Server server(pool, port);
        auto orchestrator = std::make_shared<AgentOrchestrator>(/* deps */);

        server
            .post("/v1/chat", [this](const Request& req, Response& resp) -> Task<void> {
                auto body = json::parse(req.body());
                AgentOrchestrator::Request agent_req{
                    .user_id = body.value("user_id", ""),
                    .session_id = body.value("session_id", ""),
                    .message = body.value("message", ""),
                    .context = {
                        .city = body.value("city", ""),
                        .longitude = body.value("longitude", 0.0),
                        .latitude = body.value("latitude", 0.0),
                        .device_type = body.value("device_type", "")
                    }
                };

                auto result = co_await orchestrator_>Chat(agent_req);
                resp.json(result.to_json().dump());
                co_return;
            })
            .post("/v1/feedback", [](const Request& req, Response& resp) -> Task<void> {
                // TODO
                resp.json(json_ok());
                co_return;
            })
            .get("/v1/history/", [](const Request& req, Response& resp) -> Task<void> {
                // 从路径解析 session_id
                resp.json(json_ok());
                co_return;
            });

        server.bind("0.0.0.0");
        server.start();
        co_return;
    }

private:
    std::shared_ptr<AgentOrchestrator> orchestrator_;
};

int main() {
    AgentApplication app;
    app.run(8080).result();
    return 0;
}
```

## 六、建议下一步

1. 更新 `项目规划_修订版.md` 中技术选型部分，将 HTTP Server 从 Drogon 改为 `coro::net::http::Server`。
2. 补充 JSON 库选型为 `nlohmann/json`。
3. 确定 HTTP Client 选型：Phase 0 可先使用 `cpr`（libcurl 封装）做同步调用并外包线程池，后续再替换为纯协程 Client。
4. 开始搭建基于 `coro` 的 Phase 0 工程骨架。
