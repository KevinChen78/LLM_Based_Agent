# Python dependencies — only needed for the default PostgreSQL retrieval
# backend. llm_gateway and `RETRIEVAL_BACKEND=json` remain pure stdlib.
# psycopg[binary] ships prebuilt wheels for Windows/py3.13 (no compiler needed);
# the pool extra provides the thread-safe ConnectionPool for ThreadingHTTPServer.
psycopg[binary,pool]>=3.2,<4

# Vector recall (RETRIEVAL_VECTOR=on): ONNX-runtime embeddings, no torch.
# Ships Windows/py3.13 wheels; pulls onnxruntime.
fastembed>=0.4,<1

# Learning-to-rank (Phase 2.1): ranking_service + scripts/train_ranker.py only.
# llm_gateway / e2e / generators / evaluators stay pure stdlib.
lightgbm>=4.0,<5

# gRPC pilot (Phase 5): retrieval_service gRPC front-end (GRPC_PORT, default
# off) + the protocol dimension in scripts/test_pg_retrieval.py. grpcio is the
# runtime; grpcio-tools only regenerates retrieval_service/gen/ stubs from
# proto/retrieval.proto (see scripts/gen_grpc_py.py). Everything else stays
# pure stdlib.
grpcio>=1.71,<2
grpcio-tools>=1.71,<2
